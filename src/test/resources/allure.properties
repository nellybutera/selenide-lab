# Without this, Allure defaults to writing results to ./allure-results (relative to the
# JVM's working directory) instead of target/allure-results, where allure-maven,
# `mvn allure:serve`, and the CI workflow all expect to find them.
allure.results.directory=target/allure-results
