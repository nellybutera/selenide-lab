# Keep browser tests sequential by default - each test opens its own WebDriver
# session and running them in parallel would need one browser instance per thread.
junit.jupiter.execution.parallel.enabled=false
junit.jupiter.testinstance.lifecycle.default=per_method
